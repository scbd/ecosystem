const umd    = true
const cjs    = false
const legacy = { umd, cjs }

const modern = false
const ssr    = false

const widget     = { legacy: true, modern: false }

export default {
  legacy, modern, ssr, widget
}